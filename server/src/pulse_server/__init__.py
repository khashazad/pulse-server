"""Top-level package for the pulse-server backend.

Marks ``pulse_server`` as an importable Python package. Holds no
runtime code or re-exports; submodules (``app``, ``db``, ``config``,
``routers``, ``services``, ``repositories``, etc.) are imported directly by
consumers and tests.
"""
